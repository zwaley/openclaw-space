# GitHub Pages configuration
title: OpenClaw Games
description: A collection of games built with OpenClaw
theme: jekyll-theme-cayman

# Exclude from processing.
# The following items will not be processed, by default.
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/